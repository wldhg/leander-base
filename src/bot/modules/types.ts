/* Leander is subject to the terms of the Mozilla Public License 2.0.
 * You can obtain a copy of MPL at LICENSE.md of repository root. */

interface LNDRActs {
  dialog?: import('./dialog').default['acts'];
  embed?: import('./embed').default['acts'];
  guild?: import('./guild').default['acts'];
  point?: import('./point').default['acts'];
  tools?: import('./tools').default['acts'];
  web?: import('./web').default['acts'];
}
